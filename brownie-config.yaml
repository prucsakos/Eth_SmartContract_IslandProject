dependencies:
  - OpenZeppelin/openzeppelin-contracts@4.5.0
compiler:
  solc:
    remappings:
      - '@openzeppelin=OpenZeppelin/openzeppelin-contracts@4.5.0'
dotenv: .env
networks:
  default: development
wallets:
  riskby_from_key: ${PRIVATE_KEY}
ETH_TOK:
  token: ${ETHERSCAN_TOKEN}